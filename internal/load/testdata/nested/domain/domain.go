// Package domain is a second package of the module being generated for, reached
// only because model imports it.
package domain

// Place is declared here rather than beside the declaration that names it.
type Place struct{ Name string }

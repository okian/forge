// Package other holds a subject declared somewhere other than the package that
// names it.
package other

// Session is a subject a declaration in another package is specialised to.
type Session struct {
	ID string
}
